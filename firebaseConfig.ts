// Centralized Firebase imports (no manual initializeApp). Android uses google-services.json.
// iOS uses GoogleService-Info.plist if added. Consumers should import from @react-native-firebase/* directly.
export {};
