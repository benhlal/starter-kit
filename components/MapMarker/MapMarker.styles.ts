import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  // Marker styles will be handled by react-native-maps
  // This file is here for consistency with our component structure
});
